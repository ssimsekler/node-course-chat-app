{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server/server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "test-watch": "export NODE_ENV=test || SET \"NODE_ENV=test\" && nodemon --exec 'npm test'",
    "test-debug": "export NODE_ENV=test || SET \"NODE_ENV=test\" && mocha --inspect-brk server/**/*.test.js"
  },
  "engines": {
    "node": "9.5.0"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.2"
  }
}